server:
  port: 8086

spring:
  application:
    name: ms-seguridad
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/seguridaddb
    username: admin
    password: admin123
    jpa:
      hibernate:
        ddl-auto: update
      database: PostgreSQL
      database-platform: org.hibernate.dialect.PostgreSQLDialect

  eureka:
    instance:
      hostname: localhost
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka